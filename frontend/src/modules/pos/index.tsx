import React, {useState} from 'react';
export const PosView: React.FC = () => {
  const [filter,setFilter]=useState('high');
  return <div><h2>POS - POS - checkout, tips, retail, package</h2><p>checkout</p></div>
};
export default PosView;
